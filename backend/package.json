{
  "name": "fullstack-product-inventory-aws-react-nodejs",
  "version": "1.0.0",
  "description": "Fullstack product inventory aws react nodejs application",
  "main": "handler.js",
  "scripts": {
    "deploy:dev": "npx serverless deploy --stage dev",
    "deploy-prod": "npx serverless deploy --stage prod",
    "start:offline": "npx serverless offline",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "Manish Khatri",
  "license": "ISC",
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.902.0",
    "@aws-sdk/lib-dynamodb": "^3.902.0"
  },
  "devDependencies": {
    "serverless": "^4.20.2",
    "serverless-offline": "^14.4.0"
  }
}
